name: conditions
on: [push]
jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
      - name: step-1
        if: github.ref == 'refs/heads/feature-**'
        run: echo "branch-feature"
      - name: step-2
        if: github.ref == 'refs/heads/main'
        run: echo "branch-main"