name: concurrency
on: [push]
concurrency:
  # this workflow should be triggered for any branch at only once
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true # existing will be cancelled and latest will run
jobs:
  concurrent:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: sleep 20